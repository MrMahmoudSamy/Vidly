@model  IEnumerable<Vidly.Models.Customer>
@{
    ViewBag.Title = "Index";
}

<h2>Customers</h2>

@{ 
    if(!Model.Any())
    {
        <text>We don't have any customers yet</text>
    }
    else
    {
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">Customer</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var customer in Model)
                {
                    <tr>
                        <td>
                            @Html.ActionLink(customer.CustomerName, "Details", "Customers", new { id = customer.CustomerId }, new { @class = "navbar-brand" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
}